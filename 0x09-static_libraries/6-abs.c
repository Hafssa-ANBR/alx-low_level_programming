#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check, function parameter
 * Return: the absolute value of int n or -n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

